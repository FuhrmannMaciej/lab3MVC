@model lab3MVC.Models.FormModel
@{
    ViewData["Title"] = "Form";
}

<div class="text-left">
    <h2 class="display-4">Dane z formularza</h2>
    @for (int i = 0; i < Model.NumberOfRepetitions; i++)
    {
        @Html.DisplayFor(model => model.Name)
        <br />
    }

</div>
